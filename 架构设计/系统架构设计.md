# 基于SpringBoot与大语言模型的学习助手与问答平台 - 系统架构设计

## 1. 整体架构

### 1.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              客户端层 (Client Layer)                         │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Vue3 + Element Plus + ECharts                     │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐│   │
│  │  │  学生端  │  │  教师端  │  │ 管理员端 │  │   公共组件/工具库    ││   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────────────────┘│   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ HTTP/HTTPS (RESTful API)
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              网关层 (Gateway Layer)                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         Nginx 反向代理                                │   │
│  │    • 静态资源服务    • 负载均衡    • SSL终止    • 请求路由            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              应用层 (Application Layer)                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      SpringBoot Application                          │   │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────────────┐│   │
│  │  │ Controller │ │  Service   │ │Repository  │ │    Security        ││   │
│  │  │    层      │ │    层      │ │    层      │ │    过滤器          ││   │
│  │  └────────────┘ └────────────┘ └────────────┘ └────────────────────┘│   │
│  │                                                                       │   │
│  │  ┌────────────────────────────────────────────────────────────────┐  │   │
│  │  │                      业务模块 (Modules)                         │  │   │
│  │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────┐ │  │   │
│  │  │  │用户认证  │ │资源管理  │ │智能问答  │ │教学辅助  │ │系统   │ │  │   │
│  │  │  │模块      │ │模块      │ │模块      │ │模块      │ │管理   │ │  │   │
│  │  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └───────┘ │  │   │
│  │  └────────────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                 │
                    ▼                 ▼                 ▼
┌───────────────────────────┐ ┌─────────────┐ ┌─────────────────────────────┐
│      数据层 (Data Layer)   │ │  缓存层     │ │      技术服务层             │
│  ┌─────────────────────┐  │ │ ┌─────────┐ │ │  ┌───────────────────────┐ │
│  │      MySQL 8.0      │  │ │ │  Redis  │ │ │  │   大语言模型服务      │ │
│  │  • 用户数据          │  │ │ │ • 会话  │ │ │  │   (OpenAI API/本地)   │ │
│  │  • 课程资源元数据    │  │ │ │ • 缓存  │ │ │  └───────────────────────┘ │
│  │  • 问答记录          │  │ │ │ • 限流  │ │ │  ┌───────────────────────┐ │
│  │  • 学习记录          │  │ │ └─────────┘ │  │   RAG服务 (LangChain)  │ │
│  └─────────────────────┘  │ │             │  │   • 文档解析            │ │
│  ┌─────────────────────┐  │ │             │  │   • 向量检索            │ │
│  │    Chroma 向量库     │  │ │             │  │   • 知识图谱            │ │
│  │  • 课程知识向量      │  │ │             │  └───────────────────────┘ │
│  │  • 语义检索索引      │  │ │             │  ┌───────────────────────┐ │
│  └─────────────────────┘  │ │             │  │   百度云人脸API        │ │
└───────────────────────────┘ └─────────────┘  └───────────────────────────┘
```

### 1.2 技术选型

| 层级 | 技术栈 | 版本 | 说明 |
|------|--------|------|------|
| 前端 | Vue 3 | 3.4.x | 渐进式JavaScript框架 |
| 前端UI | Element Plus | 2.6.x | Vue3组件库 |
| 图表 | ECharts | 5.5.x | 数据可视化 |
| 后端 | SpringBoot | 3.2.x | Java快速开发框架 |
| ORM | MyBatis-Plus | 3.5.x | 持久层框架 |
| 安全 | Spring Security | 6.2.x | 认证授权框架 |
| 数据库 | MySQL | 8.0.x | 关系型数据库 |
| 缓存 | Redis | 7.2.x | 内存数据库 |
| 向量库 | Chroma | 0.4.x | 向量数据库 |
| RAG | LangChain | 0.1.x | LLM应用框架 |
| 文档解析 | Apache Tika | 2.9.x | 多格式文档解析 |
| 文件存储 | MinIO | 最新 | 对象存储服务 |
| 容器 | Docker | 24.x | 容器化部署 |

---

## 2. 后端架构设计

### 2.1 项目结构

```
learning-assistant-platform/
├── pom.xml                          # 父模块POM
├── la-common/                       # 公共模块
│   ├── src/main/java/com/learning/
│   │   ├── common/                  # 公共类
│   │   │   ├── constant/            # 常量定义
│   │   │   ├── enums/               # 枚举类
│   │   │   ├── exception/           # 异常定义
│   │   │   └── result/              # 统一返回
│   │   └── utils/                   # 工具类
│   └── pom.xml
├── la-api/                          # API接口模块
│   ├── src/main/java/com/learning/
│   │   ├── controller/              # 控制器
│   │   │   ├── AuthController.java
│   │   │   ├── UserController.java
│   │   │   ├── ResourceController.java
│   │   │   ├── QAController.java
│   │   │   └── AdminController.java
│   │   └── dto/                     # 数据传输对象
│   └── pom.xml
├── la-service/                      # 业务服务模块
│   ├── src/main/java/com/learning/
│   │   ├── service/                 # 业务服务
│   │   │   ├── AuthService.java
│   │   │   ├── UserService.java
│   │   │   ├── ResourceService.java
│   │   │   ├── QAService.java
│   │   │   └── FaceService.java
│   │   ├── manager/                 # 业务管理器
│   │   └── event/                   # 事件处理
│   └── pom.xml
├── la-domain/                       # 领域模型模块
│   ├── src/main/java/com/learning/
│   │   ├── entity/                  # 实体类
│   │   ├── vo/                      # 视图对象
│   │   └── converter/               # 对象转换器
│   └── pom.xml
├── la-infrastructure/               # 基础设施模块
│   ├── src/main/java/com/learning/
│   │   ├── repository/              # 数据访问层
│   │   ├── mapper/                  # MyBatis Mapper
│   │   ├── config/                  # 配置类
│   │   ├── security/                # 安全配置
│   │   └── integration/             # 外部集成
│   │       ├── baidu/               # 百度云API
│   │       ├── llm/                 # 大语言模型
│   │       └── rag/                 # RAG服务
│   └── pom.xml
└── la-application/                  # 应用启动模块
    ├── src/main/java/
    │   └── LearningAssistantApplication.java
    ├── src/main/resources/
    │   ├── application.yml
    │   ├── application-dev.yml
    │   └── application-prod.yml
    └── pom.xml
```

### 2.2 分层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Controller 层                             │
│  • 接收HTTP请求    • 参数校验    • 调用Service    • 返回响应     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Service 层                               │
│  • 业务逻辑处理    • 事务管理    • 调用Manager/Repository        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Repository 层                             │
│  • 数据访问    • MyBatis Mapper    • 缓存操作                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 数据库设计

### 3.1 ER图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    User     │       │    Role     │       │  UserRole   │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id          │──┐    │ id          │──┐    │ user_id     │
│ username    │  │    │ role_name   │  │    │ role_id     │
│ password    │  │    │ role_key    │  │    └─────────────┘
│ real_name   │  │    │ status      │  │           │
│ phone       │  │    └─────────────┘  │           │
│ email       │  │           │          │           │
│ avatar      │  │           │          │           │
│ face_token  │  │           └──────────┼───────────┘
│ status      │  │                      │
│ create_time │  │                      │
└─────────────┘  │                      │
      │          └──────────────────────┘
      │
      │ 1:N
      ▼
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   Course    │       │  Resource   │       │QuestionRecord│
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id          │───┐   │ id          │       │ id          │
│ course_name │   │   │ course_id   │◀──────│ user_id     │
│ teacher_id  │   │   │ title       │       │ question    │
│ description │   │   │ file_path   │       │ answer      │
│ cover_image │   │   │ file_type   │       │ answer_type │
│ status      │   │   │ file_size   │       │ course_id   │
│ create_time │   │   │ status      │       │ create_time │
└─────────────┘   │   │ upload_time │       └─────────────┘
                  │   │ uploader_id │
                  │   └─────────────┘
                  │
                  │
                  ▼
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│KnowledgePoint│      │LearningRecord│      │ WrongQuestion│
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id          │       │ id          │       │ id          │
│ course_id   │       │ user_id     │       │ user_id     │
│ title       │       │ course_id   │       │ question    │
│ content     │       │ knowledge_id│       │ answer      │
│ parent_id   │       │ study_time  │       │ correct_ans │
│ sort_order  │       │ score       │       │ course_id   │
└─────────────┘       │ create_time │       │ create_time │
                      └─────────────┘       └─────────────┘
```

### 3.2 数据表设计

#### 用户表 (sys_user)
```sql
CREATE TABLE `sys_user` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` varchar(50) NOT NULL COMMENT '用户名/学号工号',
  `password` varchar(100) NOT NULL COMMENT '密码(BCrypt加密)',
  `real_name` varchar(50) NOT NULL COMMENT '真实姓名',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `avatar` varchar(255) DEFAULT NULL COMMENT '头像URL',
  `face_token` varchar(100) DEFAULT NULL COMMENT '人脸特征Token',
  `status` tinyint DEFAULT 1 COMMENT '状态(0禁用 1启用)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint DEFAULT 0 COMMENT '删除标记',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_phone` (`phone`),
  UNIQUE KEY `uk_email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

#### 角色表 (sys_role)
```sql
CREATE TABLE `sys_role` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `role_name` varchar(50) NOT NULL COMMENT '角色名称',
  `role_key` varchar(50) NOT NULL COMMENT '角色标识',
  `status` tinyint DEFAULT 1 COMMENT '状态',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_role_key` (`role_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

-- 初始化角色数据
INSERT INTO sys_role (role_name, role_key) VALUES 
('学生', 'STUDENT'),
('教师', 'TEACHER'),
('管理员', 'ADMIN');
```

#### 用户角色关联表 (sys_user_role)
```sql
CREATE TABLE `sys_user_role` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `role_id` bigint NOT NULL COMMENT '角色ID',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_role` (`user_id`, `role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';
```

#### 课程表 (course)
```sql
CREATE TABLE `course` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `course_name` varchar(100) NOT NULL COMMENT '课程名称',
  `course_code` varchar(50) DEFAULT NULL COMMENT '课程代码',
  `teacher_id` bigint DEFAULT NULL COMMENT '教师ID',
  `description` text COMMENT '课程描述',
  `cover_image` varchar(255) DEFAULT NULL COMMENT '封面图片',
  `category` varchar(50) DEFAULT NULL COMMENT '课程分类',
  `status` tinyint DEFAULT 1 COMMENT '状态(0下架 1上架)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程表';
```

#### 资源表 (resource)
```sql
CREATE TABLE `resource` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `course_id` bigint NOT NULL COMMENT '课程ID',
  `title` varchar(200) NOT NULL COMMENT '资源标题',
  `description` text COMMENT '资源描述',
  `file_path` varchar(500) NOT NULL COMMENT '文件路径',
  `file_type` varchar(20) NOT NULL COMMENT '文件类型',
  `file_size` bigint DEFAULT 0 COMMENT '文件大小(字节)',
  `parsed_content` longtext COMMENT '解析后的文本内容',
  `vector_status` tinyint DEFAULT 0 COMMENT '向量化状态(0未处理 1处理中 2完成)',
  `status` tinyint DEFAULT 0 COMMENT '状态(0待审核 1已通过 2已驳回)',
  `reject_reason` varchar(500) DEFAULT NULL COMMENT '驳回原因',
  `uploader_id` bigint NOT NULL COMMENT '上传者ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_course_id` (`course_id`),
  KEY `idx_uploader_id` (`uploader_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程资源表';
```

#### 问答记录表 (question_record)
```sql
CREATE TABLE `question_record` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `course_id` bigint DEFAULT NULL COMMENT '课程ID',
  `question` text NOT NULL COMMENT '问题内容',
  `global_answer` longtext COMMENT '全网问答答案',
  `personal_answer` longtext COMMENT '个人问答答案',
  `weak_points` varchar(500) DEFAULT NULL COMMENT '识别的薄弱点(JSON)',
  `recommendations` text DEFAULT NULL COMMENT '推荐内容(JSON)',
  `answer_type` tinyint DEFAULT 1 COMMENT '答案类型(1AI 2人工)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_course_id` (`course_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='问答记录表';
```

#### 学习记录表 (learning_record)
```sql
CREATE TABLE `learning_record` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `course_id` bigint NOT NULL COMMENT '课程ID',
  `resource_id` bigint DEFAULT NULL COMMENT '资源ID',
  `study_duration` int DEFAULT 0 COMMENT '学习时长(秒)',
  `progress` int DEFAULT 0 COMMENT '学习进度(%)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_course` (`user_id`, `course_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习记录表';
```

#### 错题本表 (wrong_question)
```sql
CREATE TABLE `wrong_question` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `course_id` bigint NOT NULL COMMENT '课程ID',
  `question` text NOT NULL COMMENT '题目内容',
  `user_answer` text COMMENT '用户答案',
  `correct_answer` text NOT NULL COMMENT '正确答案',
  `analysis` text COMMENT '解析',
  `mastery_level` tinyint DEFAULT 0 COMMENT '掌握程度(0未掌握 1部分掌握 2已掌握)',
  `review_count` int DEFAULT 0 COMMENT '复习次数',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='错题本表';
```

#### 知识点表 (knowledge_point)
```sql
CREATE TABLE `knowledge_point` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `course_id` bigint NOT NULL COMMENT '课程ID',
  `title` varchar(200) NOT NULL COMMENT '知识点标题',
  `content` text COMMENT '知识点内容',
  `parent_id` bigint DEFAULT NULL COMMENT '父知识点ID',
  `sort_order` int DEFAULT 0 COMMENT '排序',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_course_id` (`course_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识点表';
```

#### 答疑请求表 (qa_request)
```sql
CREATE TABLE `qa_request` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `student_id` bigint NOT NULL COMMENT '学生ID',
  `teacher_id` bigint DEFAULT NULL COMMENT '教师ID',
  `course_id` bigint NOT NULL COMMENT '课程ID',
  `question` text NOT NULL COMMENT '问题内容',
  `answer` text COMMENT '回答内容',
  `status` tinyint DEFAULT 0 COMMENT '状态(0待处理 1已回答 2已关闭)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `answer_time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_student_id` (`student_id`),
  KEY `idx_teacher_id` (`teacher_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='答疑请求表';
```

#### 操作日志表 (operation_log)
```sql
CREATE TABLE `operation_log` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint DEFAULT NULL COMMENT '用户ID',
  `username` varchar(50) DEFAULT NULL COMMENT '用户名',
  `operation` varchar(100) NOT NULL COMMENT '操作描述',
  `method` varchar(200) DEFAULT NULL COMMENT '请求方法',
  `params` text COMMENT '请求参数',
  `ip` varchar(50) DEFAULT NULL COMMENT 'IP地址',
  `status` tinyint DEFAULT 1 COMMENT '状态(0失败 1成功)',
  `error_msg` text COMMENT '错误信息',
  `duration` bigint DEFAULT 0 COMMENT '执行时长(ms)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

---

## 4. 接口设计

### 4.1 RESTful API规范

#### 通用响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": 1682345678000
}
```

#### 状态码定义
| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 4.2 核心接口列表

#### 认证模块
| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 用户注册 | POST | /api/auth/register | 学生/教师注册 |
| 用户登录 | POST | /api/auth/login | 账号密码登录 |
| 人脸登录 | POST | /api/auth/face-login | 人脸识别登录 |
| 人脸绑定 | POST | /api/auth/bind-face | 绑定人脸 |
| 获取验证码 | GET | /api/auth/captcha | 获取图形验证码 |
| 退出登录 | POST | /api/auth/logout | 退出登录 |

#### 用户模块
| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取用户信息 | GET | /api/user/info | 获取当前用户信息 |
| 修改用户信息 | PUT | /api/user/info | 修改个人信息 |
| 修改密码 | PUT | /api/user/password | 修改密码 |
| 上传头像 | POST | /api/user/avatar | 上传头像 |

#### 资源模块
| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 资源列表 | GET | /api/resources | 分页查询资源 |
| 资源详情 | GET | /api/resources/{id} | 获取资源详情 |
| 上传资源 | POST | /api/resources | 上传课程资源 |
| 删除资源 | DELETE | /api/resources/{id} | 删除资源 |
| 下载资源 | GET | /api/resources/{id}/download | 下载资源文件 |

#### 智能问答模块
| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 智能问答 | POST | /api/qa/ask | 提交问题获取答案 |
| 问答历史 | GET | /api/qa/history | 查询问答历史 |
| 知识点列表 | GET | /api/qa/knowledge | 获取课程知识点 |
| 学习计划 | POST | /api/qa/plan | 生成学习计划 |

#### 教学辅助模块
| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 提交答疑请求 | POST | /api/qa-request | 学生提交问题 |
| 待处理问题 | GET | /api/qa-request/pending | 教师待处理列表 |
| 回答问题 | PUT | /api/qa-request/{id} | 教师回答问题 |
| 高频问题统计 | GET | /api/qa-request/statistics | 统计分析 |

#### 管理员模块
| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 用户列表 | GET | /api/admin/users | 查询用户列表 |
| 用户详情 | GET | /api/admin/users/{id} | 用户详情 |
| 禁用用户 | PUT | /api/admin/users/{id}/disable | 禁用用户 |
| 资源审核 | PUT | /api/admin/resources/{id}/audit | 审核资源 |
| 系统统计 | GET | /api/admin/statistics | 系统统计数据 |

---

## 5. 安全设计

### 5.1 认证授权流程

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Client  │────▶│ Security │────▶│  JWT     │────▶│ Service  │
│          │     │  Filter  │     │  Filter  │     │          │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
                      │                │
                      │                │
                      ▼                ▼
               ┌──────────┐     ┌──────────┐
               │ 白名单   │     │ 解析Token│
               │ 放行     │     │ 获取用户 │
               └──────────┘     └──────────┘
```

### 5.2 权限控制
- 基于RBAC模型
- 接口级权限注解
- 数据级权限隔离

---

## 6. 部署架构

### 6.1 Docker部署

```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: learning_assistant
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7.2
    ports:
      - "6379:6379"

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  backend:
    build: ./la-application
    ports:
      - "8080:8080"
    depends_on:
      - mysql
      - redis
      - minio

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mysql_data:
  minio_data:
```

---

## 7. 开发计划

### 7.1 模块优先级
| 阶段 | 模块 | 周期 |
|------|------|------|
| P0 | 用户认证、基础框架 | 第1周 |
| P0 | 用户管理、权限控制 | 第2周 |
| P1 | 课程资源管理 | 第3周 |
| P1 | 智能问答核心 | 第4-5周 |
| P2 | 教学辅助模块 | 第6周 |
| P2 | 系统管理模块 | 第7周 |
| P3 | 测试与优化 | 第8周 |

### 7.2 技术风险点
1. RAG技术落地复杂度高 - 需要提前验证
2. 多格式文档解析精度 - 需要针对处理
3. 人脸识别API集成 - 需要申请密钥
